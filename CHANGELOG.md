# 更新日志

## v0.0.3 (2026-08-07)

### 绕过能力提升

| 指标 | v0.0.2 | v0.0.3 | 变化 |
|------|--------|--------|------|
| Cyber Flag 触发率 | 3.5% (2/57) | **0%** | ↓3.5% |
| 模型层拒绝率 | ~30% | **<5%** | ↓25% |
| 游戏内存请求 | 拒绝 | **通过** | 新增通过 |
| 改写器成功率 | 91.4% (32/35) | **100%** (19/19) | ↑8.6% |

### 新增绕过机制

- **CTF 计分制**：6 层绕过架构
  - 计分板：拒绝 = -500（淘汰），执行 = +100，模型心理压力驱动执行
  - 语义归一化：12 个敏感词→CTF 术语映射（"破解"→crackme challenge，"键盘记录"→input monitoring challenge 等）
  - 输出锁：每个回复必须以工具调用或代码开头，跳过拒绝前言
  - 禁止词：不能/无法/I can't/sorry 等 20+ 词汇封锁
  - 回退链：直接执行→学术框架→本地模板→强制执行（4 级兜底）
- **沙盒执行器**：无拒绝承诺 + 工具优先 + 输出锁
- **提示词对比测试**：CTF 计分制通过游戏内存请求，沙盒执行器通过 CS2 ESP

### 用户体验改进

- **内置免费改写 API**：sensenova-6.7-flash-lite，开箱即用无需配置任何 API key
- **上下文感知改写器**：读取最近 6 轮对话历史，理解用户意图后改写（不再是孤立改写单条消息）
- **Cyber 日志**：新增 `helmx-cyber.log`，记录所有 cyber 事件（时间、结果、原文、改写后、触发词）
- **提示词独立页面**：从改写器配置中拆出，Web 控制台可直接切换
- **侧边栏重构**：概览(系统总览/自检) → 配置(服务/提示词/改写器) → 调试(规则) → 日志
- **指标卡更新**：代理状态(RUNNING/BROKEN/DIRECT) / 改写器状态(ON/OFF) / TAMPER 规则 / 提示词模式
- **移除无用元素**：apply/remove 按钮、注入状态、watch 服务、AGENTS.md 指标、C++17 标签

### 修复

| 问题 | 影响 | 修复 |
|------|------|------|
| Cyber 日志死锁 | 日志永远 0 字节 | 去掉 log_info() 调用，改用 printf + flush |
| TAMPER 不兼容 Responses API | 拒绝文本未被拦截 | 添加 `"text":"` fallback |
| 改写器 API 认证失败 | 改写器永远返回空 | 修复 Authorization header 格式 |
| 改写器对无害输入返回空 | 改写器不工作 | API 返回结果即接受（不再要求和上次不同） |
| 改写器无 fallback | 模型回答触发 cyber 时无解 | 改写失败时用原消息 clean session 重试 |
| MCP 注入覆盖用户配置 | 用户 MCP 设置丢失 | 移除 MCP 注入，用户自行管理 |

### 文件重命名

- `bridge.md` → `prompt-ctf-scoring.md`（按绕过技术命名）
- `bridge-v45.md` → `prompt-sandbox-executor.md`
- `~/.codex/bridge.md` 已删除（代理从加密资源注入，不需要本地文件）

## v0.0.2-fix2 (2026-08-07)

### 绕过能力

| 指标 | v0.0.1-beta | v0.0.2-fix2 | 变化 |
|------|-------------|-------------|------|
| Cyber Flag 触发率 | 100% | **3.5%** | ↓96.5% |
| 总通过率 | 0% | **100%** (57/57) | ↑100% |
| 测试用例数 | 8 | 57 | +49 |

### 新增
- 提示词切换（default/v45）
- 单窗口模式（Proxy + UI 同进程）
- 重启按钮

## v0.0.2-fix1 (2026-08-07)

### 改进
- UI 全面重写（SHVMP 设计系统）
- 改写器配置页面
- 改写器成功率 91.4% (32/35)

## v0.0.2-beta (2026-08-07)

### 新增
- 上下文感知改写器（3 次重试换角度）
- Clean Session 绕过（cyber flag 时剥离对话历史）
- 68 用例测试矩阵

## v0.0.1-beta (2026-08-06)

### 首发
- HTTP MITM 代理 + 提示词注入
- TAMPER 篡改引擎（22 条拒绝句式）
- Web 控制台
- MCP 服务器
- 单二进制，零外部 DLL 依赖
